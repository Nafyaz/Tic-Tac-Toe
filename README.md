# Arbitrary sized Tic Tac Toe in Svelte

## Description



## How to run

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y nodejs unzip nginx git

bun install
bun run dev
```

## TODOs
1. Design for mobile (responsiveness with mobile-first)
2. Add zoom in / out